var expect = require('chai').expect;
var greet = require('./greet');   // Import the greet function

describe('wserver:test',function() {
    it('should work!', function() {
        expect(true).to.be.true;
    })
}); 